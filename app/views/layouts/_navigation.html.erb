<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">WizzyMailer</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <% if logged_in? %>
      <li class="nav-item active">  
        <%= link_to "My Messages", messages_path, class: "nav-link" %>
      </li>
      <% end %>
      <li class="nav-item active">
        <%= link_to "Users", users_path, class: "nav-link" %>
      </li>
      <% if logged_in? %>
        <li class="nav-item active">
          <%= link_to current_user.username, user_path(current_user), class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= button_to "Logout", logout_path, class: "nav-link btn btn-dark", method: :delete %>
        </li>
      <% else %>
        <li class="nav-item active">
          <%= link_to "Login", login_path, class: "nav-link" %>
        </li>
        <li class="nav-item active">
          <%= link_to "Sign Up", signup_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>